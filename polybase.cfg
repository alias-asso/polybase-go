[server]
host = "0.0.0.0"
port = "8080"
mode = "dev"

[database]
path = "./polybase.db" # In prod: /var/lib/polybase/polybase.db

[ldap]
host = "127.0.0.1"
port = "3893"
base_dn = "dc=localhost"
user_dn = "cn=users,dc=localhost"
admin_group = "users"

[auth]
jwt_secret = "development-secret"  # In prod: use proper secret
jwt_expiry = "24h"
