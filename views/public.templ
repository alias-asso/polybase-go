package views

import "git.sr.ht/~alias/polybase-go/internal"

templ Public(courses []internal.Course, count int) {
	@Base(false) {
		@Header(false, "", GetRandomMessage()) {
			<a href="/login">Connexion</a>
		}
		@Grid(GroupCoursesBySemesterAndKind(courses), false)
		@Footer(count)
	}
}
